import 'package:flutter/material.dart';

class WaiterView extends StatelessWidget {
  const WaiterView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}